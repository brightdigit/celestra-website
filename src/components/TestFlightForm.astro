---
// This form uses Netlify Forms for server-side handling
---

<div class="form-container">
  <form
    name="testflight-signup"
    method="POST"
    data-netlify="true"
    netlify-honeypot="bot-field"
  >
    <input type="hidden" name="form-name" value="testflight-signup" />

    <!-- Honeypot field for spam prevention -->
    <p hidden>
      <label>
        Don't fill this out if you're human: <input name="bot-field" />
      </label>
    </p>

    <div class="form-group">
      <label for="email">Email Address</label>
      <input
        type="email"
        id="email"
        name="email"
        required
        placeholder="you@example.com"
        autocomplete="email"
        maxlength="254"
      />
    </div>

    <div class="form-group">
      <label for="name">Name (Optional)</label>
      <input
        type="text"
        id="name"
        name="name"
        placeholder="Your name"
        autocomplete="name"
        maxlength="100"
      />
    </div>

    <button type="submit">Join TestFlight Waitlist</button>

    <p class="privacy-note">
      We'll only use your email to send TestFlight invitations.
      No spam, ever. See our <a href="/privacy">privacy policy</a> and <a href="/terms">terms of service</a>.
    </p>
  </form>
</div>

<style>
  .form-container {
    max-width: 500px;
    margin: 0 auto;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    background: white;
    padding: 2.5rem;
    border-radius: 1rem;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    text-align: left;
  }

  .form-group.checkbox {
    flex-direction: row;
    align-items: center;
  }

  .form-group.checkbox input {
    width: auto;
    margin-right: 0.5rem;
  }

  label {
    font-weight: 500;
    color: #374151;
  }

  input[type="email"],
  input[type="text"] {
    padding: 0.75rem;
    border: 1px solid #d1d5db;
    border-radius: 0.5rem;
    font-size: 1rem;
    transition: border-color 0.2s;
  }

  input[type="email"]:focus,
  input[type="text"]:focus {
    outline: none;
    border-color: #4AA3B0;
    box-shadow: 0 0 0 3px rgba(74, 163, 176, 0.1);
  }

  button {
    padding: 0.875rem 1.5rem;
    background: #4AA3B0;
    color: white;
    border: none;
    border-radius: 0.5rem;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s, transform 0.2s;
  }

  button:hover {
    background: #3d8a95;
    transform: translateY(-1px);
  }

  .privacy-note {
    font-size: 0.875rem;
    color: #6b7280;
    text-align: center;
    margin: 0;
  }

  .privacy-note a {
    color: #4AA3B0;
    text-decoration: none;
  }

  .privacy-note a:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    form {
      padding: 2rem;
    }
  }
</style>
