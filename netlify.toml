[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Forms configuration
[forms]
  # Email notifications when forms are submitted
  # Set BETA_EMAIL environment variable in Netlify Dashboard
  [forms.testflight-signup]
    to = "${BETA_EMAIL}"
