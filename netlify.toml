[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Forms configuration
[forms]
  # Email notifications when forms are submitted
  [forms.testflight-signup]
    to = "beta@celestr.app"
