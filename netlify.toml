[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"

# Forms configuration
[forms]
  # Email notifications when forms are submitted
  [forms.testflight-signup]
    to = "beta@celestr.app"
